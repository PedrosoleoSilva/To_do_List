import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        fontSize: 16,
        fontWeight: 600,
        borderRadius: 8,
        borderWidth: 2,
        borderColor: "#c4c2",
        width: "100%",
        height: 48,
        paddingHorizontal: 16,

    }
})

